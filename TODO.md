## To implement

- [ ] Kinetic-energy coupling for the reacting closure: `EQ_KERNEL`/`EQ_FROZEN` currently drop `u^2/2` (h ~ h_t), O(M^2) at low Mach. Restore the outer KE fixed point (R-B2.2) with the 2-inner-solve IFT (closure ift, analogous to `perfect_gas._attach_density_imag`). Note the perturbation caloric map (`characteristics.edge_caloric`) drops the KE term (`m=0`) to match this closure; restore `m=u` there too when the closure carries KE.
- [ ] Element-dropping in the `@njit` equilibrium kernel (`fns/thermo/_chem.equilibrate_hp`) for burnt edges whose elemental abundance (`Z = xi @ Zfeed`) has a zero element (e.g. a parallel branch whose products lack carbon). Mirror thermolib's keep_el/keep_sp compaction (locate-on-real). Series injection (all elements present downstream) avoids it today.
- [ ] We are now considering "convenience" elements, that will transform into multiple elements when added to network. We should be careful to preserve proper numbering. I am not certain yet, maybe we re-run the re-numbering algorithm, or just insert the new "serial" portion as incremental numbers to the correct position and shift the rest? First user: a `helmholtz_resonator(V, neck_length, neck_area)` wrapper (tee + neck duct + cavity), now buildable by hand from the primitives.
- [ ] Storage `M` remainder (the generic per-port compliance + series inertance + manifold volume are done -- `_inline_storage`/`_manifold_storage`): (a) per-branch manifold inertance (a neck length on each junction/splitter branch's `p0-pi` row); (b) heated finite-volume element's independent energy store (couples the energy/transport row to `S(omega)`). See `scratch/inertance-end-correction-theory.md` s3.3/s5.
- [ ] Storage energy-ledger hook (HR-plan Phase 4): generalize `_stored_energy` (currently duct-only) to add the lumped storage energy -- cavity/plenum `0.5 * C * |p'|^2` and inertance `0.5 * (L_eff/A) * |mdot'|^2` -- so `forced_power_balance` / `modal_energy_balance` close with storage present. Operator is already complete (`J_alg + i*omega*M + P + S`); this is the diagnostics side.
- [ ] Dedicated sudden-contraction element resolving the vena-contracta state (composite: isentropic to vena contracta + Borda re-expansion) for exact loss and minimum static pressure at higher Mach. The current `sudden_area_change` `cc`-loss uses the incompressible 1/2 rho u^2 head, accurate only to O(M^2).
- [ ] "multiple_fuel_manifold" notebook has an excellent network plot - let's develop it further.
- [x] Per-edge species + mixture-property accessors & YAML output: surface, per edge, the solved chemical species (mole/mass fractions), the local **molar mass `W`** and **`cp`** (both already computed by the equilibrium kernel en route to `rho`/`c`), and the transported scalars (mixture fractions `xi` and the burnt marker). Retain the species moles `nj` (burnt edges) / reconstruct from `n_feed = xi @ Nfeed` (frozen edges) and keep `W`,`cp` through `recover_edge` (today `eq_kernel_state_from_Z_warm` drops `nj` and returns only `T,rho,c,1/ntot`); add `W` and `cp` as standard per-edge output fields next to `T,c,M,...` (trivially constant for perfect gas), add a `Solution.species(e)`-style accessor (plus accessors for `W`,`cp`,marker), and emit all of these in `fns/io/yaml_out.py`. **YAML output should include every per-edge quantity discussed by default** -- species, `W`, `cp`, mixture fractions, and the burnt marker -- the files are small and the extra data is cheap. Today users must recompute species via `gas.equilibrate_HP` / `fns.composition`.
- [ ] Burnt-marker scalar closure (the principled fix for the flame-orientation hazard in Issues): introduce one domain-wide transported "burnt" marker scalar, shared by all flames, user-settable at inflow BCs (default 0), with a source at each flame element. Because scalar transport already rides the *signed* mass flow (`mdot_in = -orient*mdot` in `node_donor`, `kernels.py:67`), the marker lands on the genuinely-burnt edges regardless of declared edge orientation -- so it determines "downstream" robustly where the topology flood-fill cannot. Gate the closure with a **smooth** sigmoid of the marker (NOT a hard `marker>0` threshold -- a branch on the flow state breaks complex-step safety) by blending the `eq_frozen`/`eq_kernel` outputs inside `closure_solve` (`rho = (1-s)*rho_frozen + s*rho_eq`, `s = sigmoid(marker/B_ref)`), keeping it a single complex-step-safe coupled solve with no re-solve. The marker is bimodal at convergence (flame = discrete jump), so the blended state is physical except in transients; cost is the equilibrium kernel on every reacting edge (warm-started) plus one extra unknown per edge. Demote the existing topology flood-fill to the marker's initial guess. **Post-processing:** expose the marker per edge after solve via accessor + YAML (default-on), exactly like the species / `W` / `cp` output item above.
- [x] Adaptive measured-from-state residual/variable scaling: replace the fixed boundary-reference scales with scales measured from the iterate -- total mass inflow `sum(mdot)` (mass rows), total enthalpy inflow `sum(mdot*h_t)` (energy rows), total species inflow `sum(mdot*Z)` (composition rows) over inlet edges -- rebuilt once per homotopy stage. Requires lifting `res_scale`/`var_scale` out of the immutable `CompiledProblem` into the solve loop (`control.py`/`linear.py`). Demote `mdot_ref`/`h_ref` to optional internal overrides so they drop off the `Network` signature (auto-derived, still settable); this subsumes the separate absolute `h_ref` datum. Keep `p_ref` (gauge anchor) and `T_ref` (initial-guess seed). Iteration-0 seed: `sum(mdot)` if all inlets are mass-flow-specified, else a dP-based isentropic estimate `A*sqrt(2*rho*dP_max)` from boundary pressures (replaces today's pressure-blind `0.3*rho*c*A_median` guess); only needs to be order-of-magnitude right. **Guardrail -- `mdot_in = 0`:** the quiescent case (explicit `mass_flow_inlet` with `mdot=0`) must keep working -- floor each measured inflow scale at a nonzero value (the dP / M=0.3 seed, or the `p_ref`/`h_ref` defaults) so the norm never divides by zero or collapses. Shares the max-dP helper with the dP-scaled homotopy item.

## To verify

## To brainstorm

- [ ] We reject BC's when there is no absolute pressure reference from any of them. For such cases, would it work if we had an absolute pressure reference at some arbitrary edge state in the domain? If so, we could let the user enforce absolute pressure at exactly one edge.

## To discuss

- [ ] How does linear resistance element behave in mean flow?

## To test

## Issues

- [x] Defining nodes and edges through "build_problem" routine is more convenient than using our Network class, which requires one-by-one addition. Let our Network class accept, upon creation, a list of edges and nodes. Ensure this does not break the port numbering capability. The functionality offered by build_problem should move into, without degrading, the Network class constructor. (With other TODO items, I belive we won't have these "ref" quantities in there anymore, maybe only as kwargs). In principle, build_problem should not be something that the users normally access (or convince me otherwise, I can't think of any such situation). Note that this update changes a lot of stuff usages in the notebooks and potentially the tests. So be careful during updates.
- [ ] We don't have any mechanism to prevent connecting incompatible elements with each other - see the guardrails we put in the UI. Perhaps we were too strict, this is open to re-evalute.
- [x] Flame closure auto-assignment relies on declared edge orientation, not flow sign: `_resolve_edge_models` (`fns/io/yaml_in.py`) flood-fills "burnt" downstream of flames along declared tail->head arrows at compile time, and the assignment is never reconciled with the solved `mdot` sign. Since edge orientation is arbitrary by design (an edge may carry negative `mdot`), a flame edge drawn pointing toward the flame is silently mislabeled (product edge -> frozen, or reactant edge -> equilibrium) -> wrong thermochemical state on a still-converging solve. Fix (preferred): the burnt-marker scalar closure (see "To implement") -- it rides the signed mass flow, so it is orientation-proof, and the flood-fill becomes only its initial guess. Cheaper interim guard: warn at parse time when a flame node's downstream seed set is empty/asymmetric.

## Deferred

- [ ] Frequency-dependent radiation / reservoir outlet BCs: a small library of named analytic radiation-impedance constructors returning `Z(omega)` callables for `PerturbationBC.impedance` (auto-converts to `R`). Unflanged open end (Levine–Schwinger low-`ka`: `Z/(rho c) ~= (ka)^2/4 + i*0.6133*ka`), flanged/baffled-piston (`(ka)^2/2 + i*0.82*ka`), and jet-pipe with mean flow (Munt/Rienstra, Mach/Strouhal-dependent). Keep each analytic in `omega` so it continues off the real axis and works with the contour eigensolver; tabulated/measured radiation data must go through the AAA/barycentric rational fit first (else Nyquist / forced-response only). Replaces the constant `open_end` / `mean_flow_open_end` reflections for realistic open outlets.
- [ ] Condensed-phase equilibrium *products* (soot/graphite, condensed oxides). v1 products are gas-only (`SpeciesLibrary.product_mask`); condensed species are feed-only (set elements + enthalpy, masked out of the burnt slate). Related edge case: an auto-slate feed element with no gas product (a metal) leaves the burnt solve unbalanced.
- [ ] Compositional ("indirect") noise -- one gap remains. It is already captured wherever the linearization is inherited: `J_alg` carries composition->acoustic at a flame, an area change, and the inherited `choked_nozzle_outlet` element (its critical-mass-flux row is complex-stepped through composition -> the `R_xi` column for free); and scalar *ports* now read out in the scattering matrices (`response.py`). Open: the hand-written analytic compact closures (`PerturbationBC.choked_nozzle`/`constant_mass_flow`) drop `R_xi` (flagged by `CompositionalNoiseWarning`) -- complex-step the closure's composition dependence to restore it. Theory: Magri JFM 2016.
- [ ] Non-compact / through-throat nozzle-response solver: integrate the linearized-Euler perturbation ODEs through the `M(x)` profile with the M=1 sonic-regularity (L'Hopital) condition (Stow-Dowling / Duran-Moreau). Handles the sonic throat the transit-time network model cannot (`tau_- = L/(c-u) -> inf`), and is the proper distributed-source compositional-noise route. The deferred supersonic / M=1 scope.
- [ ] Re-order algorithm - deferred because current solvers do not utilize this
- [ ] `perturbation_response(freeze=...)`: a lossless frozen termination (closed stub) is ill-conditioned at its resonance (real-axis pole). Add optional auto-regularization (small wall loss / pole-skip).
