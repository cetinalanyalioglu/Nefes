name: fns
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy
  - scipy
  - numba
  - pyyaml
  - pytest
  - flake8
  - black
  # notebook support + plotting (the `jupyter` extra)
  - jupyterlab
  - notebook
  - ipykernel
  - plotly
